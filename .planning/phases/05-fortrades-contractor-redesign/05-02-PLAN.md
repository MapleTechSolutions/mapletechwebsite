---
phase: 05-fortrades-contractor-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/pages/ForTrades.jsx
  - src/components/sections/ForTradesFeatures.jsx
autonomous: true
requirements:
  - FEAT-02
must_haves:
  truths:
    - "Feature 1 (Missed Call Text Back) renders with DigitalVoiceWave graphic on LEFT, text on RIGHT"
    - "Feature 2 (Automated SMS Lead Follow-Up) renders with text on LEFT, LoopGraphic on RIGHT"
    - "Feature 3 (5-Star Magic Review Funnel) renders with PulseCallGraphic on LEFT, text on RIGHT"
    - "Feature 4 (All In One Inbox) renders with text on LEFT, DocBuilderGraphic on RIGHT"
    - "Feature 5 (Functional Website) renders with inline SVG browser-frame graphic on LEFT, text on RIGHT"
    - "Feature 6 (Local SEO) renders with text on LEFT, inline SVG map-pin graphic on RIGHT"
    - "Each feature section has 4 bullet points with orange CheckCircle icons"
    - "Each feature section ends with an inline orange text-link CTA"
    - "All 6 features render between Testimonials and Trades We Serve (at the placeholder comment)"
    - "npm run build exits 0 with no console errors"
  artifacts:
    - path: "src/pages/ForTrades.jsx"
      provides: "Complete ForTrades page — all 14 sections"
    - path: "src/components/sections/ForTradesFeatures.jsx"
      provides: "6 feature sections as a single extracted component (only created if ForTrades.jsx exceeds 400 lines)"
  key_links:
    - from: "ForTrades.jsx"
      to: "DigitalVoiceWave / LoopGraphic / PulseCallGraphic / DocBuilderGraphic"
      via: "import at top of file or ForTradesFeatures.jsx"
      pattern: "import.*DigitalVoiceWave|import.*LoopGraphic|import.*PulseCallGraphic|import.*DocBuilderGraphic"
    - from: "Feature section inline CTAs"
      to: "/why-us#contact"
      via: "<Link> from react-router-dom"
      pattern: "Link to=\"/why-us#contact\""
---

<objective>
Insert the 6 feature showcase sections into the ForTrades page at the placeholder comment left by Plan 01. Each feature is a full-width alternating two-column section (graphic + bullet text) matching the UI-SPEC layout contract exactly.

If inserting all 6 features would push ForTrades.jsx past 400 lines total, extract all feature sections into src/components/sections/ForTradesFeatures.jsx and import it at the placeholder position instead.

Purpose: Complete the page's primary conversion content — 6 distinct contractor pain-points addressed with animated visuals and bullet copy.
Output: ForTrades.jsx (complete, 14 sections) — optionally + ForTradesFeatures.jsx if line count requires extraction.
</objective>

<execution_context>
@D:\VS Projects\Maple Tech Website 2.0\.planning\phases\05-fortrades-contractor-redesign\05-UI-SPEC.md
@D:\VS Projects\Maple Tech Website 2.0\.planning\phases\05-fortrades-contractor-redesign\05-CONTEXT.md
</execution_context>

<context>
@D:\VS Projects\Maple Tech Website 2.0\.planning\ROADMAP.md
@D:\VS Projects\Maple Tech Website 2.0\CLAUDE.md

<interfaces>
<!-- Existing animated graphic components — import and use directly. No new npm packages. -->

import DigitalVoiceWave from '../components/sections/DigitalVoiceWave';
import LoopGraphic from '../components/sections/LoopGraphic';
import PulseCallGraphic from '../components/sections/PulseCallGraphic';
import DocBuilderGraphic from '../components/sections/DocBuilderGraphic';

<!-- Each graphic is a default export and takes no required props. -->
<!-- Each internally calls useLiteMotion() — no need to pass motion props. -->

From SharedComponents (already imported in Plan 01 output):
- fadeInUp, fadeInLeft, fadeInRight, scaleIn, cinematicStagger, useLiteMotion
- PremiumButton (not needed per-feature — inline Link CTAs only here)

From lucide-react (already installed):
- CheckCircle (w-5 h-5 text-orange-400 flex-shrink-0 mt-0.5)
- ArrowRight (w-4 h-4 inline ml-1)

Feature section grid class (UI-SPEC):
  grid grid-cols-1 lg:grid-cols-2 gap-16 items-center

Feature text column per-section:
  Feature name: text-2xl font-semibold text-slate-900
  Bullets list: space-y-4 mt-6
  Bullet item: flex gap-3 items-start
  Bullet icon: CheckCircle w-5 h-5 text-orange-400 flex-shrink-0 mt-0.5
  Bullet text: text-base text-slate-700 leading-relaxed
  Inline CTA: <Link to="/why-us#contact" className="inline-flex items-center text-orange-400 font-semibold hover:text-orange-300 transition-colors mt-6">Let's talk — 20 minutes <ArrowRight className="w-4 h-4 ml-1" /></Link>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check Plan 01 line count and decide extraction strategy</name>

  <read_first>
    - D:\VS Projects\Maple Tech Website 2.0\src\pages\ForTrades.jsx (output from Plan 01 — count lines, locate placeholder comment)
    - D:\VS Projects\Maple Tech Website 2.0\.planning\phases\05-fortrades-contractor-redesign\05-01-SUMMARY.md (if present — check line count reported)
  </read_first>

  <files>src/pages/ForTrades.jsx</files>

  <action>
Count the current line count of src/pages/ForTrades.jsx (from Plan 01).

Decision rule:
- If current lines + estimated 6 feature sections (≈ 50 lines each = ~300 lines) would exceed 400 total → extract features to src/components/sections/ForTradesFeatures.jsx
- If total stays under 400 → insert feature sections directly into ForTrades.jsx at the placeholder comment

**Either way**, locate the exact placeholder comment in ForTrades.jsx:
```
{/* === FEATURE SECTIONS — inserted by Plan 02 (05-02-PLAN.md) === */}
```

Replace that comment with either:
a) The 6 feature sections inline (if line count is safe), OR
b) A single `<ForTradesFeatures />` component import + usage (if extracting)

Record this decision in a comment at the top of the file:
```jsx
// Plan 02: Feature sections [inline | extracted to ForTradesFeatures.jsx] — line count decision
```
  </action>

  <verify>
    <automated>wc -l src/pages/ForTrades.jsx 2>/dev/null || (Get-Content src/pages/ForTrades.jsx).Count</automated>
  </verify>

  <acceptance_criteria>
    - grep "FEATURE SECTIONS — inserted by Plan 02" src/pages/ForTrades.jsx returns NO matches (placeholder has been replaced)
    - If extracting: ForTradesFeatures.jsx exists in src/components/sections/
    - If inline: feature section content appears directly in ForTrades.jsx between Testimonials and Trades We Serve
  </acceptance_criteria>

  <done>Placeholder comment is gone. Feature sections are either inline or extracted. Decision is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Build all 6 feature sections with exact copy, graphics, and bullet structure</name>

  <read_first>
    - D:\VS Projects\Maple Tech Website 2.0\.planning\phases\05-fortrades-contractor-redesign\05-UI-SPEC.md (section "Feature Section Layout", "Component Specifications", graphic assignment table, CTA contract table)
    - D:\VS Projects\Maple Tech Website 2.0\.planning\phases\05-fortrades-contractor-redesign\05-CONTEXT.md (D-09, D-14, D-15 — feature order, bullet format, copy tone)
    - D:\VS Projects\Maple Tech Website 2.0\src\components\sections\DigitalVoiceWave.jsx (confirm default export name)
    - D:\VS Projects\Maple Tech Website 2.0\src\components\sections\LoopGraphic.jsx (confirm default export name)
    - D:\VS Projects\Maple Tech Website 2.0\src\components\sections\PulseCallGraphic.jsx (confirm default export name)
    - D:\VS Projects\Maple Tech Website 2.0\src\components\sections\DocBuilderGraphic.jsx (confirm default export name)
    - D:\VS Projects\Maple Tech Website 2.0\src\components\shared\SharedComponents.jsx (fadeInUp, fadeInLeft, fadeInRight, scaleIn, cinematicStagger, useLiteMotion exports)
  </read_first>

  <files>
    src/pages/ForTrades.jsx
    src/components/sections/ForTradesFeatures.jsx
  </files>

  <action>
Write the 6 feature sections. Destination is determined by Task 1 (inline in ForTrades.jsx OR in ForTradesFeatures.jsx). If writing ForTradesFeatures.jsx, it is a React component that accepts no props and returns a React Fragment `<>...</>` wrapping all 6 sections.

**LAYOUT RULE (UI-SPEC — MANDATORY):**
- Odd features (1, 3, 5): `[Graphic LEFT] [Text RIGHT]` — graphic in first grid column, text in second
- Even features (2, 4, 6): `[Text LEFT] [Graphic RIGHT]` — text in first grid column, graphic in second
- On mobile (< lg): columns stack vertically (grid-cols-1), graphic always appears first in DOM order regardless of visual column swap

Grid class: `grid grid-cols-1 lg:grid-cols-2 gap-16 items-center`

For even features, use `lg:order-first` on the text column and `lg:order-last` on the graphic column to achieve the swap without DOM reordering (preserves accessibility reading order on mobile).

**Section wrapper per feature:**
```jsx
<section aria-label="[Feature Name]" className="bg-[X] py-24">
  <div className="max-w-5xl mx-auto px-6">
    <div className="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
      {/* graphic column */}
      {/* text column */}
    </div>
  </div>
</section>
```

Animate text column with `motion.div variants={fadeInUp} initial="hidden" whileInView="show" viewport={{ once: true, margin: "-80px" }}` wrapper.
All motion elements must check `useLiteMotion()`. Call `const { liteMotion } = useLiteMotion();` once in the parent component.

---

**FEATURE 1 — Missed Call Text Back**
Section bg: `bg-white`
Layout: Graphic LEFT, Text RIGHT (odd)
Graphic: `<DigitalVoiceWave />`
Feature name: "Missed Call Text Back"
Section headline: `<h2 className="text-3xl font-black text-slate-900 mb-4">Missed Call Text Back</h2>`
Sub: `<p className="text-base text-slate-500 mb-6">Everyone misses calls, but not everyone texts back. Be the one who does.</p>`

4 bullets:
1. "A new lead calls while you're on a job. The system texts them back in under 60 seconds — before they call the next guy on the list."
2. "Completely automatic. When your number misses a call, the message goes out. No app to open, no button to press."
3. "Without this, you lose the job before you even know it existed. With it, you're in the conversation while you're still on the roof."
4. "Runs 24/7 — evenings, weekends, holidays. Your business never goes to voicemail again."

Inline CTA: `<Link to="/why-us#contact" className="inline-flex items-center text-orange-400 font-semibold hover:text-orange-300 transition-colors mt-6">Let's talk — 20 minutes <ArrowRight className="w-4 h-4 ml-1" /></Link>`

---

**FEATURE 2 — Automated SMS Lead Follow-Up**
Section bg: `bg-slate-50`
Layout: Text LEFT, Graphic RIGHT (even)
Graphic: `<LoopGraphic />`
Feature name: "Automated SMS Lead Follow-Up"
Section headline: `<h2 className="text-3xl font-black text-slate-900 mb-4">Automated SMS Lead Follow-Up</h2>`
Sub: `<p className="text-base text-slate-500 mb-6">Most contractors follow up once. Their leads wait 3 days. Yours get a text in 10 minutes.</p>`

4 bullets:
1. "When a new lead comes in — from your website, a form, or a missed call — the system sends a follow-up text automatically, within minutes."
2. "The sequence runs on a schedule you set: day 1, day 3, day 7. Friendly, professional, and already written for you."
3. "Most jobs are lost because nobody followed up. The lead moved on. This system makes sure that doesn't happen."
4. "You stay focused on the job site. The system keeps the pipeline warm."

Inline CTA: same pattern as Feature 1.

---

**FEATURE 3 — 5-Star Magic Review Funnel**
Section bg: `bg-white`
Layout: Graphic LEFT, Text RIGHT (odd)
Graphic: `<PulseCallGraphic />`
Feature name: "5-Star Magic Review Funnel"
Section headline: `<h2 className="text-3xl font-black text-slate-900 mb-4">5-Star Magic Review Funnel</h2>`
Sub: `<p className="text-base text-slate-500 mb-6">You can't make everyone happy, but the system sure can ask the right people at the right time.</p>`

4 bullets:
1. "After a job wraps, the system sends a review request to your customer automatically. They tap a link and land directly on your Google review page."
2. "Timing is everything — the ask goes out when the customer is happiest, right after the work is done."
3. "Without a system, most contractors never ask. The happy customers say nothing. The one unhappy customer leaves a review."
4. "One new 5-star review a week compounds fast. More reviews mean better ranking, more trust, and more jobs."

Inline CTA: same pattern.

---

**FEATURE 4 — All In One Inbox**
Section bg: `bg-slate-50`
Layout: Text LEFT, Graphic RIGHT (even)
Graphic: `<DocBuilderGraphic />`
Feature name: "All In One Inbox"
Section headline: `<h2 className="text-3xl font-black text-slate-900 mb-4">All In One Inbox</h2>`
Sub: `<p className="text-base text-slate-500 mb-6">Leads from 6 different places — one screen. No missed DMs, no forgotten texts.</p>`

4 bullets:
1. "Every lead message — from your website, Google, Facebook, or SMS — lands in one inbox. One place to check, one place to reply."
2. "You reply from the app or the desktop. The customer sees a message from your business number, not your personal cell."
3. "Without it, leads fall through the cracks between platforms. A Facebook DM sits unseen for three days while the job goes to someone else."
4. "You own the conversation history. Every lead, every message, every follow-up in one place."

Inline CTA: same pattern.

---

**FEATURE 5 — Functional Website**
Section bg: `bg-white`
Layout: Graphic LEFT, Text RIGHT (odd)
Graphic: Inline SVG — browser frame with maple leaf favicon (build this inline, no new component file needed)

```jsx
{/* Functional Website SVG — browser frame + maple leaf favicon */}
<div className="w-full h-[320px] flex items-center justify-center">
  <svg viewBox="0 0 320 220" className="w-full max-w-sm" fill="none" xmlns="http://www.w3.org/2000/svg">
    {/* Browser chrome */}
    <rect x="8" y="8" width="304" height="204" rx="10" fill="#1e293b" />
    <rect x="8" y="8" width="304" height="36" rx="10" fill="#334155" />
    <rect x="8" y="30" width="304" height="14" fill="#334155" />
    {/* Traffic lights */}
    <circle cx="28" cy="26" r="5" fill="#ef4444" />
    <circle cx="44" cy="26" r="5" fill="#f59e0b" />
    <circle cx="60" cy="26" r="5" fill="#22c55e" />
    {/* Address bar */}
    <rect x="76" y="18" width="180" height="16" rx="8" fill="#475569" />
    <text x="166" y="30" fontSize="8" fill="#94a3b8" textAnchor="middle" fontFamily="monospace">yourtradessite.ca</text>
    {/* Favicon dot */}
    <circle cx="84" cy="26" r="4" fill="#fb923c" />
    {/* Page content area */}
    <rect x="24" y="56" width="160" height="12" rx="4" fill="#fb923c" opacity="0.8" />
    <rect x="24" y="76" width="120" height="8" rx="4" fill="#475569" opacity="0.6" />
    <rect x="24" y="92" width="136" height="8" rx="4" fill="#475569" opacity="0.4" />
    <rect x="24" y="108" width="100" height="8" rx="4" fill="#475569" opacity="0.3" />
    {/* CTA button block */}
    <rect x="24" y="128" width="80" height="24" rx="6" fill="#fb923c" />
    <text x="64" y="144" fontSize="8" fill="white" textAnchor="middle" fontFamily="sans-serif" fontWeight="bold">Book a call</text>
    {/* Right image block */}
    <rect x="200" y="56" width="96" height="100" rx="8" fill="#334155" opacity="0.7" />
    {/* Maple leaf in image block */}
    <text x="248" y="120" fontSize="36" textAnchor="middle" fill="#fb923c" opacity="0.4">🍁</text>
  </svg>
</div>
```

Note: The maple leaf emoji in SVG is a fallback — if it renders poorly, replace with a simple polygon maple shape using the path from SharedComponents.jsx: `M50 5 L55 20 L70 15 ...`. Keep it minimal.

Feature name: "Functional Website"
Section headline: `<h2 className="text-3xl font-black text-slate-900 mb-4">A Website That Actually Works For You</h2>`
Sub: `<p className="text-base text-slate-500 mb-6">Not just a page that exists — a page that captures leads, earns trust, and shows up on Google.</p>`

4 bullets:
1. "A professional website built for your trade — fast, mobile-first, and designed to turn visitors into phone calls."
2. "Your contact form connects directly to your inbox. Every submission lands in your unified inbox with a follow-up sequence attached."
3. "Most trade websites look like they were built in 2011. A clean, modern site signals you're serious — and that you're still in business."
4. "You own it. No monthly website builder fee stacked on top. It's part of the system."

Inline CTA: same pattern.

---

**FEATURE 6 — Local SEO**
Section bg: `bg-slate-50`
Layout: Text LEFT, Graphic RIGHT (even)
Graphic: Inline SVG — Google Maps-style search result bars

```jsx
{/* Local SEO SVG — search results with map pins */}
<div className="w-full h-[320px] flex items-center justify-center">
  <svg viewBox="0 0 300 240" className="w-full max-w-sm" fill="none" xmlns="http://www.w3.org/2000/svg">
    {/* Search bar */}
    <rect x="20" y="16" width="260" height="32" rx="16" fill="#f1f5f9" stroke="#cbd5e1" strokeWidth="1.5" />
    <text x="44" y="36" fontSize="11" fill="#64748b" fontFamily="sans-serif">plumber near me</text>
    <circle cx="32" cy="32" r="6" stroke="#94a3b8" strokeWidth="1.5" fill="none" />
    <line x1="37" y1="37" x2="42" y2="42" stroke="#94a3b8" strokeWidth="1.5" strokeLinecap="round" />

    {/* Result 1 — highlighted (your business) */}
    <rect x="20" y="64" width="260" height="52" rx="8" fill="#fff7ed" stroke="#fb923c" strokeWidth="1.5" />
    {/* Map pin orange */}
    <circle cx="40" cy="90" r="10" fill="#fb923c" />
    <text x="40" y="94" fontSize="10" fill="white" textAnchor="middle" fontFamily="sans-serif" fontWeight="bold">1</text>
    <rect x="58" y="74" width="120" height="8" rx="4" fill="#1e293b" opacity="0.8" />
    <rect x="58" y="88" width="80" height="6" rx="3" fill="#fb923c" opacity="0.6" />
    <rect x="58" y="100" width="60" height="6" rx="3" fill="#64748b" opacity="0.4" />
    {/* Stars */}
    <text x="146" y="95" fontSize="9" fill="#fb923c" fontFamily="sans-serif">★★★★★</text>

    {/* Result 2 */}
    <rect x="20" y="126" width="260" height="48" rx="8" fill="white" stroke="#e2e8f0" strokeWidth="1" />
    <circle cx="40" cy="150" r="8" fill="#94a3b8" />
    <text x="40" y="154" fontSize="9" fill="white" textAnchor="middle" fontFamily="sans-serif">2</text>
    <rect x="56" y="136" width="100" height="7" rx="3" fill="#1e293b" opacity="0.5" />
    <rect x="56" y="150" width="70" height="5" rx="2.5" fill="#64748b" opacity="0.3" />
    <rect x="56" y="162" width="50" height="5" rx="2.5" fill="#64748b" opacity="0.2" />

    {/* Result 3 */}
    <rect x="20" y="184" width="260" height="42" rx="8" fill="white" stroke="#e2e8f0" strokeWidth="1" />
    <circle cx="40" cy="205" r="8" fill="#cbd5e1" />
    <text x="40" y="209" fontSize="9" fill="white" textAnchor="middle" fontFamily="sans-serif">3</text>
    <rect x="56" y="194" width="88" height="7" rx="3" fill="#1e293b" opacity="0.35" />
    <rect x="56" y="208" width="60" height="5" rx="2.5" fill="#64748b" opacity="0.2" />

    {/* "Your business" label */}
    <text x="220" y="78" fontSize="8" fill="#fb923c" fontFamily="sans-serif" fontWeight="bold">Your business</text>
    <line x1="218" y1="80" x2="180" y2="88" stroke="#fb923c" strokeWidth="1" strokeDasharray="3 2" />
  </svg>
</div>
```

Feature name: "Local SEO"
Section headline: `<h2 className="text-3xl font-black text-slate-900 mb-4">Show Up When Customers Search</h2>`
Sub: `<p className="text-base text-slate-500 mb-6">This one takes time — but so does every investment worth making.</p>`

4 bullets:
1. "When someone in your city searches 'plumber near me' or 'HVAC repair Saskatoon', you want your business on that first page. We build toward that."
2. "We set up and optimize your Google Business Profile, manage your citations, and build the local signals Google uses to rank you."
3. "Most contractors have an unclaimed Google Business listing with no photos, no hours, and no reviews. That's free real estate they're leaving on the table."
4. "This is the long game — results build over months, not days. We'll be honest with you about timelines. It's worth doing right."

Inline CTA: same pattern.

---

**CONSTRAINTS (all 6 features):**
- No window.location.href — all CTAs use `<Link to="/why-us#contact">`
- No inline styles — Tailwind classes only
- useLiteMotion() called once in parent component (not inside each feature)
- All animated motion.div wrappers use `whileInView` with `viewport={{ once: true, margin: "-80px" }}`
- CheckCircle icon on every bullet: `<CheckCircle className="w-5 h-5 text-orange-400 flex-shrink-0 mt-0.5" aria-hidden="true" />`
- ArrowRight icon in inline CTAs: `<ArrowRight className="w-4 h-4 ml-1" aria-hidden="true" />`
- Each `<section>` has `aria-label` matching feature name
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - grep "Missed Call Text Back" src/pages/ForTrades.jsx src/components/sections/ForTradesFeatures.jsx 2>/dev/null returns at least 1 match
    - grep "Automated SMS Lead Follow-Up" src/pages/ForTrades.jsx src/components/sections/ForTradesFeatures.jsx 2>/dev/null returns at least 1 match
    - grep "5-Star Magic Review Funnel" src/pages/ForTrades.jsx src/components/sections/ForTradesFeatures.jsx 2>/dev/null returns at least 1 match
    - grep "All In One Inbox" src/pages/ForTrades.jsx src/components/sections/ForTradesFeatures.jsx 2>/dev/null returns at least 1 match
    - grep "Functional Website\|A Website That Actually" src/pages/ForTrades.jsx src/components/sections/ForTradesFeatures.jsx 2>/dev/null returns at least 1 match
    - grep "Local SEO\|Show Up When" src/pages/ForTrades.jsx src/components/sections/ForTradesFeatures.jsx 2>/dev/null returns at least 1 match
    - grep "DigitalVoiceWave" src/pages/ForTrades.jsx src/components/sections/ForTradesFeatures.jsx 2>/dev/null returns at least 1 match
    - grep "LoopGraphic" src/pages/ForTrades.jsx src/components/sections/ForTradesFeatures.jsx 2>/dev/null returns at least 1 match
    - grep "PulseCallGraphic" src/pages/ForTrades.jsx src/components/sections/ForTradesFeatures.jsx 2>/dev/null returns at least 1 match
    - grep "DocBuilderGraphic" src/pages/ForTrades.jsx src/components/sections/ForTradesFeatures.jsx 2>/dev/null returns at least 1 match
    - grep -c "CheckCircle" src/pages/ForTrades.jsx src/components/sections/ForTradesFeatures.jsx 2>/dev/null shows CheckCircle used (4 bullets × 6 features = 24 occurrences across the file(s))
    - grep "Let's talk — 20 minutes" src/pages/ForTrades.jsx src/components/sections/ForTradesFeatures.jsx 2>/dev/null returns 6 matches
    - grep "window.location.href" src/pages/ForTrades.jsx src/components/sections/ForTradesFeatures.jsx 2>/dev/null returns no matches
    - grep "FEATURE SECTIONS — inserted by Plan 02" src/pages/ForTrades.jsx returns NO matches (placeholder removed)
    - npm run build exits 0 with no errors
  </acceptance_criteria>

  <done>All 6 feature sections are built with correct alternating layout, exact copy, assigned animated graphics, 4 bullets per feature with CheckCircle icons, and orange inline CTA links. Build passes cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → page | Static content — no input, no data, no auth on this page |
| Inline SVG | SVG content is static, hardcoded in JSX — no dynamic data, no XSS vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | Inline SVG graphics | accept | SVG is static JSX, no user-controlled content, no data injection vector. |
| T-05-02-02 | Spoofing | Feature CTAs | accept | All 6 inline CTAs use React Router Link — no external redirect, no window.location leaks. |
</threat_model>

<verification>
1. Visit /for-trades in local dev — scroll past Testimonials and verify 6 feature sections appear in order: Missed Call, SMS Follow-Up, Review Funnel, All In One Inbox, Functional Website, Local SEO
2. Verify alternating layout on desktop (lg+): Feature 1 graphic LEFT / Feature 2 text LEFT / Feature 3 graphic LEFT / Feature 4 text LEFT / Feature 5 graphic LEFT / Feature 6 text LEFT
3. Verify each feature has exactly 4 bullets with orange CheckCircle icons visible
4. Click each "Let's talk — 20 minutes →" link — all navigate to /why-us#contact
5. Resize to mobile — verify columns stack (graphic above text) for all 6 features
6. npm run build exits 0 with no console errors
7. If ForTradesFeatures.jsx was created — verify ForTrades.jsx imports it and it renders at the correct position (between Testimonials and Trades We Serve)
</verification>

<success_criteria>
- All 6 feature sections render between Testimonials and Trades We Serve sections
- Odd features (1, 3, 5) show graphic in left column on desktop
- Even features (2, 4, 6) show graphic in right column on desktop (text-first on mobile)
- Each feature has 4 bullets with CheckCircle icons in text-orange-400
- Each feature ends with "Let's talk — 20 minutes →" in orange text-link style
- DigitalVoiceWave, LoopGraphic, PulseCallGraphic, DocBuilderGraphic are all rendered
- Inline SVGs for Feature 5 and Feature 6 are visible and meaningful (not blank)
- Zero window.location.href references in any new/modified file
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-fortrades-contractor-redesign/05-02-SUMMARY.md` documenting:
- Whether features were inlined or extracted to ForTradesFeatures.jsx, and why
- Final line count of ForTrades.jsx
- Final line count of ForTradesFeatures.jsx (if created)
- Any copy deviations from UI-SPEC (none expected)
- Build result (pass/fail)
</output>
