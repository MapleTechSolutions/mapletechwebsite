---
phase: 05-fortrades-contractor-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/ForTrades.jsx
autonomous: true
requirements:
  - FEAT-02
must_haves:
  truths:
    - "Hero loads with headline 'You're losing jobs you don't even know about.' on dark bg-slate-900"
    - "Stats strip shows < 60 sec, 2 weeks, 0 training in orange-400 numerals"
    - "Three testimonial cards render in a 3-column grid on bg-slate-50, first card voices skepticism"
    - "Trades We Serve grid shows all 8 trade chips in pill style"
    - "How It Works shows 3 steps with exact parenthetical copy from D-19"
    - "Why Maple Tech shows 6 cards with exact titles and body text from D-11"
    - "FAQ accordion shows all 5 questions; clicking a question expands it with orange left-border"
    - "Final CTA section shows 'Ready to stop losing jobs to your voicemail?' on bg-slate-900"
    - "All CTAs use <Link to='/why-us#contact'> — no window.location.href"
    - "Helmet is the first child of the component return"
  artifacts:
    - path: "src/pages/ForTrades.jsx"
      provides: "Page skeleton — Hero through Final CTA (non-feature sections)"
      exports: ["default ForTrades"]
  key_links:
    - from: "ForTrades.jsx"
      to: "/why-us#contact"
      via: "Link component from react-router-dom"
      pattern: "Link to=\"/why-us#contact\""
    - from: "ForTrades.jsx"
      to: "PremiumButton"
      via: "import from SharedComponents"
      pattern: "PremiumButton variant=\"orange\""
---

<objective>
Delete the Phase 4 ForTrades.jsx entirely and rebuild the page skeleton: Hero, Stats Strip, Testimonials, Trades We Serve, How It Works, Why Maple Tech, FAQ accordion, and Final CTA sections. Feature sections (the 6 animated showcases) are built in Plan 02.

Purpose: Establish the light/dark alternating page structure, exact copy, and all non-feature sections. Plan 02 depends on this file existing with the correct imports and shell.
Output: src/pages/ForTrades.jsx (foundation — Feature sections come in Plan 02)
</objective>

<execution_context>
@D:\VS Projects\Maple Tech Website 2.0\.planning\phases\05-fortrades-contractor-redesign\05-UI-SPEC.md
@D:\VS Projects\Maple Tech Website 2.0\.planning\phases\05-fortrades-contractor-redesign\05-CONTEXT.md
</execution_context>

<context>
@D:\VS Projects\Maple Tech Website 2.0\.planning\ROADMAP.md
@D:\VS Projects\Maple Tech Website 2.0\.planning\STATE.md
@D:\VS Projects\Maple Tech Website 2.0\CLAUDE.md

<interfaces>
<!-- Imports the executor MUST use. No codebase exploration needed. -->

From src/components/shared/SharedComponents.jsx:
```jsx
import {
  PremiumButton,
  fadeInUp,
  fadeInLeft,
  fadeInRight,
  scaleIn,
  cinematicStagger,
  useLiteMotion,
} from '../components/shared/SharedComponents';
```

PremiumButton usage:
```jsx
<Link to="/why-us#contact">
  <PremiumButton variant="orange">Book a free 20-min call</PremiumButton>
</Link>
```

Animation wrapper pattern (use on every animated element):
```jsx
const { liteMotion } = useLiteMotion();
<motion.div
  variants={fadeInUp}
  initial="hidden"
  whileInView="show"
  viewport={{ once: true, margin: "-80px" }}
>
```

From react-helmet-async (already installed):
```jsx
import { Helmet } from 'react-helmet-async';
// Must be first child of component return
<Helmet>
  <title>Maple Tech — Automation for Saskatchewan Contractors</title>
  <meta name="description" content="Missed calls, slow follow-ups, no reviews — Maple Tech fixes the 3 biggest leaks in your contractor business. Built for Saskatchewan trades." />
</Helmet>
```

From react-router-dom (already installed):
```jsx
import { Link } from 'react-router-dom';
```

From lucide-react (already installed):
```jsx
import { CheckCircle, ChevronDown } from 'lucide-react';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old ForTrades.jsx and write the foundation page (Hero → FAQ → Final CTA, feature slot reserved)</name>

  <read_first>
    - D:\VS Projects\Maple Tech Website 2.0\src\pages\ForTrades.jsx (current file — understand what to DELETE, then discard entirely)
    - D:\VS Projects\Maple Tech Website 2.0\.planning\phases\05-fortrades-contractor-redesign\05-UI-SPEC.md (exact Tailwind classes, exact copy for every section)
    - D:\VS Projects\Maple Tech Website 2.0\.planning\phases\05-fortrades-contractor-redesign\05-CONTEXT.md (tone, copy decisions D-07 through D-24)
    - D:\VS Projects\Maple Tech Website 2.0\src\components\shared\SharedComponents.jsx (PremiumButton, fadeInUp, fadeInLeft, fadeInRight, scaleIn, cinematicStagger, useLiteMotion — confirm export names before using)
  </read_first>

  <files>src/pages/ForTrades.jsx</files>

  <action>
DELETE the entire content of src/pages/ForTrades.jsx and write a fresh file from scratch. Do NOT patch. The new file must implement all sections listed below in order:

**FILE STRUCTURE (top-down):**

```
imports
const ForTrades = () => {
  // FAQ state: const [openFaq, setOpenFaq] = useState(null);
  // const { liteMotion } = useLiteMotion();

  return (
    <>
      <Helmet> ... </Helmet>           ← FIRST CHILD, always

      {/* 1. Hero */}
      {/* 2. Stats Strip */}
      {/* 3. Testimonials */}
      {/* === FEATURE SECTIONS PLACEHOLDER — Plan 02 inserts here === */}
      {/* 4. Trades We Serve */}
      {/* 5. How It Works */}
      {/* 6. Why Maple Tech */}
      {/* 7. FAQ */}
      {/* 8. Final CTA */}
    </>
  );
};
export default ForTrades;
```

---

**SECTION 1 — Hero** (`bg-slate-900 py-32`)
Inner container: `max-w-5xl mx-auto px-6 text-center`
- Eyebrow tag: `<p className="text-orange-400 text-sm font-semibold tracking-widest uppercase mb-4">Trades & Contractors</p>`
- Headline: `<h1 className="text-5xl lg:text-6xl font-black text-white leading-tight mb-6">You're losing jobs you don't even know about.</h1>`
- Sub-copy: `<p className="text-xl text-slate-300 max-w-2xl mx-auto mb-10">We've heard the stories. Agency promises the moon, charges $3k, delivers 4 leads. That's not us. We show you the system before you spend a dollar.</p>`
- CTA: `<Link to="/why-us#contact"><PremiumButton variant="orange">Book a free 20-min call</PremiumButton></Link>`
- Animate hero text with `motion.div variants={cinematicStagger}` container, `motion.div variants={fadeInUp}` for each child element. useLiteMotion() gating required.

---

**SECTION 2 — Stats Strip** (`bg-slate-900 border-b border-slate-700`)
Inner: `<div className="max-w-5xl mx-auto px-6 flex flex-col sm:flex-row items-center justify-center gap-8 sm:gap-16 py-10">`

Three stats (render as array or inline — whichever is cleaner):
| Value | Label |
|---|---|
| `< 60 sec` | Text-back speed |
| `2 weeks` | Time to go live |
| `0 training` | Required from you |

Stat value: `text-4xl font-black text-orange-400`
Stat label: `text-sm font-semibold text-slate-400 uppercase tracking-wider mt-1`
Each stat wrapped in a `<div className="text-center">`.
Wrap the whole strip in a `motion.div variants={fadeInUp}` with whileInView trigger.

---

**SECTION 3 — Testimonials** (`bg-slate-50 py-24`)
Inner: `max-w-5xl mx-auto px-6`
- Section headline: `<h2 className="text-3xl font-black text-slate-900 text-center mb-12">What contractors are saying</h2>`
- Grid: `grid grid-cols-1 md:grid-cols-3 gap-6`

Three testimonial cards. Card class: `bg-white rounded-2xl p-6 shadow-sm border border-slate-100`
Quote: `text-base text-slate-700 leading-relaxed italic mb-4`
Name: `text-sm font-semibold text-slate-900`
Trade/City: `text-sm text-slate-500`

**Card 1 (skeptic first — per D-16 and Stone Systems' trust pattern):**
Quote: "Honestly, I thought it was another one of those agencies selling dreams. We were getting maybe two calls a week and losing half of them. Now the system texts back in under a minute. Signed two jobs last week that we would've missed."
Name: "— Mike T."
Trade: "HVAC | Regina, SK"

**Card 2:**
Quote: "I'm not a tech guy at all. Thought setting this up would be a nightmare. Took 20 minutes on a call and two weeks later we were live. My phone rings, I miss it, customer gets a text — it just works."
Name: "— Dave K."
Trade: "Electrician | Saskatoon, SK"

**Card 3:**
Quote: "Reviews were the thing holding us back. We do great work but nobody was leaving reviews. The follow-up system gets us a new Google review almost every week now. Makes a difference when people search."
Name: "— Sandra L."
Trade: "Painter | Moose Jaw, SK"

After the grid, centered CTA: `<div className="text-center mt-12"><Link to="/why-us#contact"><PremiumButton variant="orange">See how it works — no pressure</PremiumButton></Link></div>`
Animate cards with `motion.div variants={scaleIn}` inside a `motion.div variants={cinematicStagger}` container.

---

**FEATURE SECTIONS PLACEHOLDER:**
Add a comment exactly:
```jsx
{/* === FEATURE SECTIONS — inserted by Plan 02 (05-02-PLAN.md) === */}
```
Plan 02 will insert the 6 feature sections here.

---

**SECTION 4 — Trades We Serve** (`bg-white py-24`)
Inner: `max-w-5xl mx-auto px-6 text-center`
Headline: `<h2 className="text-3xl font-black text-slate-900 mb-4">Built for your trade</h2>`
Sub: `<p className="text-base text-slate-500 mb-10">If you run a trade business in Saskatchewan, this was built for you.</p>`
Grid: `grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-3 max-w-2xl mx-auto`

Eight chips, each: `<div className="bg-slate-100 rounded-full px-4 py-2 text-sm font-semibold text-slate-700 text-center">`
Chips: Roofers | Plumbers | HVAC Technicians | Electricians | Handymen | Painters | Landscapers | Concrete & Excavation

---

**SECTION 5 — How It Works** (`bg-slate-900 py-24`)
Inner: `max-w-5xl mx-auto px-6`
Headline: `<h2 className="text-3xl font-black text-white text-center mb-16">Here's exactly how it works</h2>`
Grid: `grid grid-cols-1 md:grid-cols-3 gap-8`

Three step cards. Use `<motion.div variants={fadeInUp}>` per card with stagger.

Step card inner structure:
- Step number: `<div className="text-5xl font-black text-orange-400/20 mb-4">01</div>`
- Headline: `<h3 className="text-xl font-semibold text-white mb-2">Free 20-min call</h3>`
- Parenthetical: `<p className="text-base text-slate-400 leading-relaxed">`

**Exact copy per step (D-19):**
Step 1:
  - Number: "01"
  - Headline: "Free 20-min call"
  - Body: "It's actually a sales call, we just didn't want to scare you. We'll hear about your business, show you the system live, and tell you exactly what we'd build."

Step 2:
  - Number: "02"
  - Headline: "We build your system"
  - Body: "7–10 business days. You fill out one simple form. We do the rest."

Step 3:
  - Number: "03"
  - Headline: "You're live"
  - Body: "30-min walkthrough. You'll need to know one thing: how to check your new inbox. We handle everything else."

After the grid: `<div className="text-center mt-12"><Link to="/why-us#contact"><PremiumButton variant="orange">Book a free 20-min call</PremiumButton></Link></div>`

---

**SECTION 6 — Why Maple Tech** (`bg-slate-800 py-24`)
Inner: `max-w-5xl mx-auto px-6`
Headline: `<h2 className="text-3xl font-black text-white text-center mb-4">Why we're different...<span className="text-slate-400 font-semibold"> (you've heard that before, right?)</span></h2>`
Sub: `<p className="text-base text-slate-400 text-center mb-12">Fair. Here's what's actually different.</p>`
Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`

Six cards, each: `<motion.div className="bg-slate-700/50 rounded-xl p-6" variants={fadeInUp}>`
Title: `text-lg font-semibold text-white mb-2`
Body: `text-sm text-slate-300 leading-relaxed`

**Exact six cards (D-11):**
1. Title: "Simple To Use" | Body: "No tech skills needed. If you can check a text, you can use this."
2. Title: "Affordable" | Body: "Designed to keep you, not lock you in. If it's not working, you shouldn't be paying."
3. Title: "No Lock-In" | Body: "Cancel anytime. No contracts. No penalties. We keep clients by doing good work."
4. Title: "No BS" | Body: "We tell you exactly what we'll build — then we build it. No surprises on the invoice."
5. Title: "Saskatchewan-Based" | Body: "You're not a ticket number. You're a neighbour. We pick up the phone."
6. Title: "We Answer" | Body: "Call us when you need us. Real humans. Not a support queue."

After grid: `<div className="text-center mt-12"><Link to="/why-us#contact"><PremiumButton variant="orange">Stop losing jobs to voicemail →</PremiumButton></Link></div>`
Wrap cards in `motion.div variants={cinematicStagger}` container.

---

**SECTION 7 — FAQ** (`bg-white py-24`)
Inner: `max-w-3xl mx-auto px-6`
Headline: `<h2 className="text-3xl font-black text-slate-900 text-center mb-12">Frequently asked questions</h2>`

State: `const [openFaq, setOpenFaq] = useState(null);`
Toggle: `onClick={() => setOpenFaq(openFaq === i ? null : i)}`

**Five FAQ items (D-20) — exact questions:**
1. Q: "When will I start seeing results?"
   A: "Most clients see their first automated text-back within the first day of going live. Missed call recovery is immediate — if someone called and left, the system texts them within 60 seconds. Review requests take a few weeks to build momentum. SEO takes longer — 3–6 months minimum. We'll be honest with you about what to expect from each piece."

2. Q: "Why do I need this if word of mouth is working?"
   A: "Word of mouth is great — until it isn't. Two extra jobs a month from a lead who found you on Google, or from a call you didn't miss, pays for this system many times over. Word of mouth gets you repeat customers. This system gets you new ones."

3. Q: "Is there a long-term contract?"
   A: "No. Cancel anytime, no penalties. We don't believe in locking people in — if the system isn't working for your business, you shouldn't be paying for it."

4. Q: "What happens if I miss a call and the system texts them — is it weird?"
   A: "Not at all. The message is written to sound professional and personal — something like 'Hey, this is [Your Business], sorry we missed your call — what can we help you with?' Customers respond well to it. It shows you care, even when you're on a job."

5. Q: "What if I'm not tech-savvy?"
   A: "You press two buttons. We set everything else up. The walkthrough takes 30 minutes and by the end you'll know everything you need to: how to check your inbox and how to send a review request. That's it."

**Accordion visual treatment (D-21):**
Each item container: `border-b border-slate-200`
Question button: `w-full flex justify-between items-center py-4 text-base font-semibold text-slate-900 text-left` with `aria-expanded={openFaq === i}` and `aria-controls={faq-answer-i}`
ChevronDown icon: `text-slate-400 w-5 h-5 transition-transform duration-200` — rotate 180deg when open: add `rotate-180` class when `openFaq === i`
Answer container (when open): `<div id="faq-answer-i" className="border-l-4 border-orange-400 pl-4 pb-4"><p className="text-base text-slate-700 leading-relaxed">`

---

**SECTION 8 — Final CTA** (`bg-slate-900 py-24`)
Inner: `max-w-3xl mx-auto px-6 text-center`
Headline: `<h2 className="text-4xl font-black text-white mb-4">Ready to stop losing jobs to your voicemail?</h2>`
Sub: `<p className="text-base text-slate-300 mb-8">Book a free 20-minute call. We'll show you the system live — before you spend a dollar.</p>`
CTA: `<Link to="/why-us#contact"><PremiumButton variant="orange">Book a free 20-min call</PremiumButton></Link>`

---

**CONSTRAINTS:**
- No window.location.href — all navigation via `<Link to="/why-us#contact">`
- No inline styles — Tailwind classes only
- useLiteMotion() called once at component top; `const { liteMotion } = useLiteMotion();`
- Helmet is literally the first JSX element returned
- Each `<section>` gets `aria-label` attribute describing its purpose
- FAQ accordion buttons have `aria-expanded` and `aria-controls`
- If file approaches 400 lines after Plan 02 is merged, executor of Plan 02 must extract features to ForTradesFeatures.jsx — but for this plan, stay in one file
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - grep -n "Helmet" src/pages/ForTrades.jsx returns a match on line 1 of the return statement (first child)
    - grep "window.location.href" src/pages/ForTrades.jsx returns no matches
    - grep "Link to=\"/why-us#contact\"" src/pages/ForTrades.jsx returns at least 4 matches (hero + testimonials + how-it-works + why-maple-tech + final-cta)
    - grep "PremiumButton variant=\"orange\"" src/pages/ForTrades.jsx returns at least 4 matches
    - grep "You're losing jobs you don't even know about" src/pages/ForTrades.jsx returns exactly 1 match
    - grep "Free 20-min call" src/pages/ForTrades.jsx returns a match
    - grep "We build your system" src/pages/ForTrades.jsx returns a match
    - grep "You're live" src/pages/ForTrades.jsx returns a match
    - grep "7–10 business days" src/pages/ForTrades.jsx returns a match
    - grep "Saskatchewan-Based" src/pages/ForTrades.jsx returns a match
    - grep "No Lock-In" src/pages/ForTrades.jsx returns a match
    - grep "When will I start seeing results" src/pages/ForTrades.jsx returns a match
    - grep "Is there a long-term contract" src/pages/ForTrades.jsx returns a match
    - grep "not tech-savvy" src/pages/ForTrades.jsx returns a match
    - grep "Ready to stop losing jobs to your voicemail" src/pages/ForTrades.jsx returns a match
    - grep "useLiteMotion" src/pages/ForTrades.jsx returns a match
    - grep "whileInView" src/pages/ForTrades.jsx returns at least 3 matches
    - grep "viewport={{ once: true" src/pages/ForTrades.jsx returns at least 3 matches
    - grep "aria-label" src/pages/ForTrades.jsx returns at least 5 matches (one per section)
    - grep "aria-expanded" src/pages/ForTrades.jsx returns at least 1 match (FAQ accordion)
    - npm run build exits 0 with no errors
  </acceptance_criteria>

  <done>ForTrades.jsx is a fresh file with Hero → Stats Strip → Testimonials → [Feature Placeholder Comment] → Trades We Serve → How It Works → Why Maple Tech → FAQ → Final CTA. All sections use exact copy from UI-SPEC. Build passes cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → page | Static marketing page — no user input, no data storage, no auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | CTA links (all 7+) | accept | All links use React Router Link — no external URLs, no window.location leaks. Static page with no sensitive data. |
| T-05-02 | Spoofing | None | accept | No auth boundary on this page — it is a public marketing page. |
</threat_model>

<verification>
1. Visit /for-trades in local dev — page renders all 8 sections in correct light/dark alternating order
2. Click every CTA — all navigate to /why-us#contact (no broken links, no window.location.href)
3. Click each FAQ item — it expands with orange left-border; clicking again collapses it
4. Check browser tab — title reads "Maple Tech — Automation for Saskatchewan Contractors"
5. npm run build exits 0 with no console errors
</verification>

<success_criteria>
- /for-trades renders with dark Hero + dark Stats Strip + light Testimonials + light/dark Trades We Serve + dark How It Works + dark Why Maple Tech + light FAQ + dark Final CTA
- All 5 FAQ questions are present and toggle correctly
- All 3 How It Works steps show exact parenthetical copy from D-19
- All 6 Why Maple Tech cards show exact titles from D-11
- Zero window.location.href references
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-fortrades-contractor-redesign/05-01-SUMMARY.md` documenting:
- Sections built
- Line count of ForTrades.jsx after this plan (critical: Plan 02 needs to know if file is approaching 400 lines)
- Any deviations from UI-SPEC (none expected)
- Exact placeholder comment text used so Plan 02 can locate the insertion point
</output>
