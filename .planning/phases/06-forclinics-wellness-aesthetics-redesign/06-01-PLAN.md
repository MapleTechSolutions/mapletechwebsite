---
phase: 06-forclinics-wellness-aesthetics-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/ForClinics.jsx
autonomous: true
requirements:
  - FEAT-03

must_haves:
  truths:
    - "/for-clinics renders without errors — npm run build exits 0"
    - "Hero displays headline 'Your software should fit your clinic. Not the other way around.' with teal accent"
    - "Pain/Gaps section lists all five clinic-specific gaps with red X icons and closing italic line"
    - "Custom CRM card grid renders all four outcome cards with teal icon containers"
    - "Comparison table renders five rows: off-the-shelf (red X) vs Maple Tech (teal checkmark)"
    - "Social proof strip renders three representative testimonials with teal left-border accent"
    - "Part 2 pivot intro section renders on dark bg with exact pivot-line copy"
    - "How the Build Works renders four numbered timeline steps on dark background"
    - "Final CTA section renders with split layout and 'What you walk away from the call with' right panel"
    - "ForClinicsFeatures stub is imported and rendered between the Part 2 intro and the How the Build Works section"
    - "All CTAs use <Link to='/why-us#contact'> — zero window.location.href"
    - "useLiteMotion() is called once at the top of the component"
    - "No inline styles anywhere — Tailwind classes only"
  artifacts:
    - path: "src/pages/ForClinics.jsx"
      provides: "Rebuilt ForClinics page — Hero through Final CTA with ForClinicsFeatures stub imported"
      min_lines: 280
  key_links:
    - from: "src/pages/ForClinics.jsx"
      to: "src/components/sections/ForClinicsFeatures"
      via: "import ForClinicsFeatures"
      pattern: "import ForClinicsFeatures"
    - from: "All CTA buttons"
      to: "/why-us#contact"
      via: "<Link to='/why-us#contact'>"
      pattern: "to=\"/why-us#contact\""
---

<objective>
Delete the Phase 4 ForClinics.jsx and rebuild from scratch — all sections from Hero (1) through Final CTA (10), with a stub import for ForClinicsFeatures (which plan 06-02 will build). The new page speaks directly to wellness and aesthetics clinic owners, uses the teal visual identity from 06-UI-SPEC.md, and funnels every CTA to /why-us#contact.

Purpose: Establish the complete page shell with all non-flywheel sections so plan 06-02 can slot in the three Part 2 feature sections cleanly.
Output: src/pages/ForClinics.jsx — rebuilt from scratch, ~280–380 lines.
</objective>

<execution_context>
@D:\VS Projects\Maple Tech Website 2.0\.planning\phases\06-forclinics-wellness-aesthetics-redesign\06-UI-SPEC.md
@D:\VS Projects\Maple Tech Website 2.0\.agents\skills\huashu-design\SKILL.md
</execution_context>

<context>
@D:\VS Projects\Maple Tech Website 2.0\.planning\ROADMAP.md
@D:\VS Projects\Maple Tech Website 2.0\.planning\phases\06-forclinics-wellness-aesthetics-redesign\06-CONTEXT.md
@D:\VS Projects\Maple Tech Website 2.0\.planning\phases\06-forclinics-wellness-aesthetics-redesign\06-UI-SPEC.md

<interfaces>
<!-- Key exports / patterns extracted from codebase. Executor: use directly — no exploration needed. -->

From src/components/shared/SharedComponents.jsx (confirmed exports):
```jsx
export { PremiumButton }         // variant="primary" → teal/cyan gradient. NEVER variant="orange" on this page.
export { fadeInUp }              // Framer Motion variant: hidden→show, y: 20→0
export { fadeInLeft }            // Framer Motion variant: hidden→show, x: -20→0
export { fadeInRight }           // Framer Motion variant: hidden→show, x: 20→0
export { scaleIn }               // Framer Motion variant: hidden→show, scale: 0.9→1
export { cinematicStagger }      // Framer Motion container variant with staggerChildren
export { useLiteMotion }         // Returns { liteMotion: boolean, disableInfiniteLoops: boolean }
export { SectionDivider }        // Optional divider component
export { MapleLeafOutline }      // SVG decorative component (used in ForTrades)
```

useLiteMotion() usage pattern (call once at top of component):
```jsx
const { liteMotion, disableInfiniteLoops } = useLiteMotion();
```

PremiumButton + Link pattern (ALL CTAs on this page):
```jsx
<Link to="/why-us#contact">
  <PremiumButton variant="primary">Book a free discovery call</PremiumButton>
</Link>
```

Section wrapper pattern (from 06-UI-SPEC.md):
```jsx
// Standard section (most sections):
<section className="py-24 bg-white" aria-label="...">
  <div className="max-w-5xl mx-auto px-6">...</div>
</section>

// Hero:
<section className="pt-32 pb-20 bg-slate-900 relative overflow-hidden" aria-label="...">
  <div className="absolute inset-0 bg-[radial-gradient(circle_at_30%_50%,rgba(20,184,166,0.08),transparent_50%),radial-gradient(circle_at_70%_30%,rgba(6,182,212,0.06),transparent_50%)]" />
  <div className="relative max-w-4xl mx-auto px-6 text-center">...</div>
</section>

// Part 2 pivot (bridge section — shorter):
<section className="py-20 bg-slate-900" aria-label="...">
  <div className="max-w-3xl mx-auto px-6 text-center">...</div>
</section>
```

whileInView animation pattern (all sections except hero):
```jsx
<motion.div
  variants={fadeInUp}
  initial="hidden"
  whileInView="show"
  viewport={{ once: true, margin: "-80px" }}
>
```

Hero animation pattern (fires on page load, not scroll):
```jsx
<motion.div variants={cinematicStagger} initial="hidden" animate="show">
  <motion.p variants={fadeInUp}>...</motion.p>
  <motion.h1 variants={fadeInUp}>...</motion.h1>
  <motion.p variants={fadeInUp}>...</motion.p>
  <motion.div variants={fadeInUp}>...</motion.div>
</motion.div>
```

From src/pages/ForClinics.jsx (Phase 4 — DELETE this, do NOT patch):
- Hero radial gradient overlay pattern: className="absolute inset-0 bg-[radial-gradient(...)]"
- Comparison table structure at lines 155–199 — use as structural starting point for upgrade

Phase 4 ForClinics import list (reference for what already existed):
```jsx
import { CheckCircle, Calendar, Users, Smartphone, BarChart3, X, Bell, Star, RotateCcw } from 'lucide-react';
import { PremiumButton, fadeInUp, scaleIn } from '../components/shared/SharedComponents';
```

ForTradesFeatures.jsx import pattern (mirror for ForClinicsFeatures stub):
```jsx
// In ForTrades.jsx:
import ForTradesFeatures from '../components/sections/ForTradesFeatures';
// Rendered at the correct position in JSX:
<ForTradesFeatures />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Phase 4 ForClinics.jsx and write rebuilt page shell — Hero through Part 2 pivot with ForClinicsFeatures stub</name>

  <read_first>
    - src/pages/ForClinics.jsx (read the full file to understand what exists — then delete and replace)
    - src/pages/ForTrades.jsx (lines 1–100 — study import block, useLiteMotion call, Helmet SEO, Hero JSX pattern)
    - .planning/phases/06-forclinics-wellness-aesthetics-redesign/06-UI-SPEC.md (primary implementation blueprint — read all sections from Hero through Part 2 pivot)
    - .planning/phases/06-forclinics-wellness-aesthetics-redesign/06-CONTEXT.md (locked decisions D-01 through D-13)
    - src/components/shared/SharedComponents.jsx (lines 1–30 — confirm export names before importing)
  </read_first>

  <action>
Delete src/pages/ForClinics.jsx in full. Write the new file from scratch. Do NOT patch the Phase 4 output.

**File structure:**

```
import block (top)
const data arrays (gaps, crmCards, comparisonRows, testimonials, timelineSteps)
export default function ForClinics()
  Helmet (SEO — first child)
  Section 1: Hero
  Section 2: Pain / Gaps
  Section 3: Part 1 CRM Card Grid
  Section 4: Comparison Table
  Section 5A: Social Proof Strip (bg-slate-50, py-16)
  Section 5B: Part 2 Intro (pivot line, bg-slate-900, py-20)
  <ForClinicsFeatures />   ← stub import — plan 06-02 builds this file
  Section 9: How the Build Works
  Section 10: Final CTA
```

**Import block:**
```jsx
import { Link } from 'react-router-dom';
import { motion } from 'framer-motion';
import { Helmet } from 'react-helmet-async';
import { Calendar, Users, Smartphone, BarChart3, CheckCircle, X, ArrowRight } from 'lucide-react';
import ForClinicsFeatures from '../components/sections/ForClinicsFeatures';
import {
  PremiumButton,
  fadeInUp,
  scaleIn,
  cinematicStagger,
  useLiteMotion,
} from '../components/shared/SharedComponents';
```

**Data arrays (declare outside the component function):**

gaps array (five strings):
```
"You can't customize the intake form to match how your consultations actually run"
"Rebooking reminders are generic — there's no way to personalize by treatment type or client history"
"There's no client-facing loyalty system that works the way your pricing model does"
"Reporting gives you booking counts, not revenue per client or real retention data"
"Your team works around the software instead of the software working around your team"
```

crmCards array (four objects: { icon, title, body }):
```
{ icon: Calendar, title: "Booking & intake built your way", body: "Custom booking flows, intake forms, and consent logic that match how your team actually works — not a generic template." }
{ icon: Users, title: "Client retention on autopilot", body: "Treatment-specific follow-ups and rebooking reminders triggered by visit history. You set it once. It runs forever." }
{ icon: Smartphone, title: "Your brand in every touchpoint", body: "Every confirmation, reminder, and client-facing message shows your name and your look — not a third-party platform's." }
{ icon: BarChart3, title: "Revenue & retention reporting", body: "See revenue per client, retention rate, treatment popularity, and exactly where your pipeline is leaking — not just booking counts." }
```

comparisonRows array (five objects: { left, right }):
```
{ left: "Generic intake fields you can't adapt to your consultation process", right: "Custom intake forms, consent flows, and medical history fields built around your exact services" }
{ left: "One reminder template for every client, every appointment", right: "Treatment-specific reminders sent at the right time, in your brand voice" }
{ left: "Booking counts and occupancy — nothing about revenue or retention", right: "Revenue per client, retention rate, treatment popularity, and pipeline leak visibility" }
{ left: "Your clients see the software company's logo, not yours", right: "Every client-facing touchpoint — booking, confirmation, app — shows only your brand" }
{ left: "Loyalty and membership features locked behind an expensive tier", right: "A full branded loyalty and referral system built into your client-facing app from day one" }
```

testimonials array (three objects: { quote, name, role }):
```
{ quote: "We were on Vagaro for three years. The difference in how our clients actually feel when they interact with us now — it's night and day. Our rebooking rate went up in the first month.", name: "Sarah K.", role: "Owner, Lumina Skin Studio" }
{ quote: "I was nervous about switching. Maple Tech made the whole build feel simple. We were live in three weeks and I haven't had to touch the intake forms since.", name: "Priya M.", role: "Founder, Serene Aesthetics" }
{ quote: "The reporting alone was worth it. For the first time, I actually know which treatments are making me money and which ones I should drop.", name: "Jessica T.", role: "Director, The Wellness Collective" }
```

timelineSteps array (four objects: { number, title, body }):
```
{ number: "1", title: "Discovery Call", body: "We learn how your clinic runs — services, client flow, current software, and what's driving your team mad. Free. No obligation." }
{ number: "2", title: "Intake & Design", body: "We map your client journey and design everything around your brand. You approve before we build a single thing." }
{ number: "3", title: "Build", body: "We build every piece: booking flow, intake forms, branded app, loyalty logic, reminders, reporting. You get a full walkthrough before launch." }
{ number: "4", title: "Launch", body: "We go live and stay close for the first two weeks. Any tweaks — we handle them." }
```

**Inside ForClinics() component — first line:**
```jsx
const { liteMotion, disableInfiniteLoops } = useLiteMotion();
```

**Helmet (SEO — first child of return):**
```jsx
<Helmet>
  <title>Maple Tech — Custom CRM &amp; Client Experience for Wellness Clinics</title>
  <meta name="description" content="Medspas, skin clinics, and lash studios: Maple Tech builds custom booking, intake, loyalty, and referral systems around your brand — not a generic template." />
  <link rel="canonical" href="https://mapletech.solutions/for-clinics" />
</Helmet>
```

**Section 1 — Hero (bg-slate-900, pt-32 pb-20):**
- Outer section: `className="pt-32 pb-20 bg-slate-900 relative overflow-hidden" aria-label="Hero — ForClinics wellness and aesthetics"`
- Radial gradient overlay div: `className="absolute inset-0 bg-[radial-gradient(circle_at_30%_50%,rgba(20,184,166,0.08),transparent_50%),radial-gradient(circle_at_70%_30%,rgba(6,182,212,0.06),transparent_50%)]"`
- Inner container: `className="relative max-w-4xl mx-auto px-6 text-center"`
- Animation: `<motion.div variants={cinematicStagger} initial="hidden" animate="show">` wrapping all content
- Eyebrow: `<motion.p variants={fadeInUp} className="text-sm font-bold uppercase tracking-[0.2em] text-teal-400 mb-4">For Wellness & Aesthetics</motion.p>`
- H1: `<motion.h1 variants={fadeInUp} className="text-4xl md:text-6xl font-black text-white tracking-tight mb-6">Your software should fit<br /><span className="text-teal-400">your clinic. Not the other way around.</span></motion.h1>`
- Sub-copy: `<motion.p variants={fadeInUp} className="text-xl text-slate-300 max-w-2xl mx-auto mb-10 leading-relaxed">Generic booking tools were built for every type of business. Which means they're perfect for none of them. We build the system around how your team actually consults, books, retains, and grows.</motion.p>`
- CTA: `<motion.div variants={fadeInUp}><Link to="/why-us#contact"><PremiumButton variant="primary">Book a discovery call</PremiumButton></Link></motion.div>`

**Section 2 — Pain / Gaps (bg-white, py-24):**
- Section: `className="py-24 bg-white" aria-label="Pain points — why generic software fails clinics"`
- Inner: `className="max-w-3xl mx-auto px-6"`
- Headline: `<motion.h2 variants={fadeInUp} ...className="text-3xl md:text-4xl font-black text-slate-900 mb-4 text-center">Your current software wasn't built for clinics like yours.</motion.h2>`
- Sub-copy: `className="text-slate-500 text-lg text-center max-w-2xl mx-auto mb-10"` — "These are the five gaps clinic owners tell us about every week."
- Gap list container: `className="rounded-3xl border border-slate-200 bg-slate-50 p-8 space-y-4"`
- Each gap row: `className="flex items-start gap-3"` with icon container `className="w-5 h-5 rounded-full bg-red-100 flex items-center justify-center flex-shrink-0 mt-0.5"` + `<X className="text-red-400 w-3 h-3" />` + `<span className="text-slate-700 leading-relaxed text-base">{gap}</span>`
- Closing italic line: `className="text-center text-slate-500 mt-8 italic text-sm"` — "You've probably tried workarounds. Spreadsheets, extra tools, manual chasing. We eliminate all of that."
- CTA below: `<div className="text-center mt-10"><Link to="/why-us#contact"><PremiumButton variant="primary">See how we fix this</PremiumButton></Link></div>`
- Wrap headline + sub + gap list + italic + CTA in `<motion.div variants={fadeInUp} initial="hidden" whileInView="show" viewport={{ once: true, margin: "-80px" }}>`

**Section 3 — Part 1: Custom CRM Card Grid (bg-slate-50, py-24):**
- Section: `className="py-24 bg-slate-50" aria-label="Custom CRM capabilities"`
- Inner: `className="max-w-5xl mx-auto px-6"`
- Eyebrow: `className="text-sm font-semibold uppercase tracking-[0.15em] text-teal-600 mb-3 text-center"` — "CUSTOM CRM / OPERATIONS"
- Headline: `className="text-4xl md:text-5xl font-black text-slate-900 mb-3 text-center"` — "Built for how your clinic actually runs"
- Sub-copy: `className="text-slate-500 text-lg text-center mb-12"` — "Custom-built, not configured. Every piece reflects your brand, your terminology, and your process."
- Card grid: `<motion.div variants={cinematicStagger} initial="hidden" whileInView="show" viewport={{ once: true, margin: "-80px" }} className="grid grid-cols-1 sm:grid-cols-2 gap-6">`
- Each card: `<motion.div variants={scaleIn} className="rounded-2xl border border-teal-100 bg-gradient-to-b from-white to-teal-50/30 p-6 hover:shadow-lg hover:shadow-teal-100/50 transition-all duration-300 group">`
  - Icon container: `className="w-11 h-11 rounded-xl bg-gradient-to-br from-teal-400 to-cyan-500 flex items-center justify-center mb-4"` with `<Icon className="w-5 h-5 text-white" />`
  - Title: `className="text-lg font-semibold text-slate-900 mb-2"`
  - Body: `className="text-sm text-slate-600 leading-relaxed"`

**Section 4 — Comparison Table (bg-white, py-24):**
- Section: `className="py-24 bg-white" aria-label="Generic software vs Maple Tech comparison"`
- Inner: `className="max-w-4xl mx-auto px-6"`
- Headline: `className="text-3xl md:text-4xl font-black text-slate-900 mb-3 text-center"` — "Off-the-shelf software vs. Maple Tech"
- Sub-copy: `className="text-slate-500 text-lg text-center mb-10"` — "Generic tools are built for every business. That means they fit none of them."
- Table wrapper: `<motion.div variants={fadeInUp} initial="hidden" whileInView="show" viewport={{ once: true, margin: "-80px" }} className="rounded-2xl overflow-hidden border border-slate-200 shadow-sm">`
  - Header row: `<div className="grid grid-cols-2">` with `<div className="bg-slate-100 p-4 text-center"><span className="text-sm font-semibold text-slate-500 uppercase tracking-wider">Off-the-shelf booking software</span></div>` and `<div className="bg-teal-600 p-4 text-center"><span className="text-sm font-semibold text-white uppercase tracking-wider">What Maple Tech builds for you</span></div>`
  - Each row: `<div className="grid grid-cols-2 border-b border-slate-100">`
    - Left cell: `className="bg-slate-50 p-5 flex items-start gap-3"` with `<X className="text-red-400 w-4 h-4 flex-shrink-0 mt-0.5" aria-hidden="true" />` + `<span className="text-sm text-slate-500 leading-relaxed">{row.left}</span>`
    - Right cell: `className="bg-teal-50 p-5 flex items-start gap-3"` with `<CheckCircle className="text-teal-500 w-4 h-4 flex-shrink-0 mt-0.5" aria-hidden="true" />` + `<span className="text-sm text-teal-800 leading-relaxed font-medium">{row.right}</span>`
- CTA below table: `<div className="text-center mt-10"><Link to="/why-us#contact"><PremiumButton variant="primary">Let's build something better</PremiumButton></Link></div>`

**Section 5A — Social Proof Strip (bg-slate-50, py-16):**
- Section: `className="py-16 bg-slate-50" aria-label="Client testimonials"`
- Inner: `className="max-w-5xl mx-auto px-6"`
- `<motion.div variants={cinematicStagger} initial="hidden" whileInView="show" viewport={{ once: true, margin: "-80px" }} className="grid grid-cols-1 md:grid-cols-3 gap-6">`
- Each card: `<motion.div variants={fadeInUp} className="bg-white rounded-2xl p-6 shadow-sm border border-slate-100">`
  - Quote block: `<div className="border-l-4 border-teal-400 pl-4 mb-4"><p className="text-base text-slate-700 leading-relaxed italic">{testimonial.quote}</p></div>`
  - Name: `<p className="text-sm font-semibold text-slate-900">{testimonial.name}</p>`
  - Role: `<p className="text-sm text-slate-500">{testimonial.role}</p>`

**Section 5B — Part 2 Pivot Intro (bg-slate-900, py-20):**
- Section: `className="py-20 bg-slate-900" aria-label="The client growth flywheel — introduction"`
- Inner: `className="max-w-3xl mx-auto px-6 text-center"`
- `<motion.div variants={cinematicStagger} initial="hidden" whileInView="show" viewport={{ once: true, margin: "-80px" }}>`
  - Eyebrow: `<motion.p variants={fadeInUp} className="text-sm font-semibold uppercase tracking-[0.15em] text-teal-400 mb-4">THE CLIENT GROWTH FLYWHEEL</motion.p>`
  - Headline line 1: `<motion.h2 variants={fadeInUp} className="text-4xl md:text-5xl font-black text-white mb-0">Most clinics spend money to get new clients.</motion.h2>`
  - Headline line 2 (pivot): `<motion.p variants={fadeInUp} className="text-4xl md:text-5xl font-black text-teal-400 mb-6">The smart ones turn existing clients into their growth engine.</motion.p>`
  - Sub-copy: `<motion.p variants={fadeInUp} className="text-lg text-slate-300 max-w-2xl mx-auto mt-6 leading-relaxed">The CRM is the foundation. What we build on top of it is what sets your clinic apart — a branded client experience that rewards loyalty and turns happy clients into your best marketing channel.</motion.p>`

**ForClinicsFeatures stub position:**
Immediately after the closing `</section>` of Section 5B (Part 2 pivot), render:
```jsx
<ForClinicsFeatures />
```
This renders nothing until plan 06-02 creates the file. Create a minimal stub file at `src/components/sections/ForClinicsFeatures.jsx` with:
```jsx
const ForClinicsFeatures = () => null;
export default ForClinicsFeatures;
```
This prevents import errors while 06-02 is not yet executed.

**Section 9 — How the Build Works (bg-slate-900, py-24):**
- Section: `className="py-24 bg-slate-900" aria-label="How the build works — four-step timeline"`
- Inner: `className="max-w-5xl mx-auto px-6"`
- Eyebrow: `className="text-sm font-semibold uppercase tracking-wider text-teal-400 text-center mb-2"` — "Most clinics are live in 3–4 weeks"
- Headline: `className="text-3xl md:text-4xl font-black text-white mb-12 text-center"` — "How the build works"
- `<motion.div variants={cinematicStagger} initial="hidden" whileInView="show" viewport={{ once: true, margin: "-80px" }} className="grid grid-cols-1 md:grid-cols-4 gap-8">`
- Each step: `<motion.div variants={fadeInUp} className="text-center">`
  - Number circle: `<div className="w-10 h-10 rounded-full bg-teal-500 flex items-center justify-center mb-4 mx-auto"><span className="text-white font-black text-sm">{step.number}</span></div>`
  - Title: `<h3 className="text-white font-semibold mb-2">{step.title}</h3>`
  - Body: `<p className="text-slate-400 text-sm leading-relaxed">{step.body}</p>`
- CTA below: `<div className="text-center mt-12"><Link to="/why-us#contact"><PremiumButton variant="primary">Book a free discovery call</PremiumButton></Link></div>`

**Section 10 — Final CTA (bg-gradient-to-br from-slate-900 to-teal-950, py-24):**
- Section: `className="py-24 bg-gradient-to-br from-slate-900 to-teal-950" aria-label="Book a free discovery call"`
- Inner: `className="max-w-4xl mx-auto px-6"`
- `<motion.div variants={fadeInUp} initial="hidden" whileInView="show" viewport={{ once: true, margin: "-80px" }} className="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">`
  - Left column:
    - Headline: `className="text-3xl md:text-4xl font-black text-white mb-4"` — "Ready to build something your clients actually feel?"
    - Sub-copy: `className="text-slate-400 text-lg mb-8 leading-relaxed"` — "Book a free call. We'll look at your current setup and show you exactly what we'd build — before you spend anything."
    - CTA: `<Link to="/why-us#contact"><PremiumButton variant="primary">Book a free discovery call</PremiumButton></Link>`
  - Right column (decorative context card):
    - Container: `className="bg-white/5 rounded-2xl border border-white/10 p-6"`
    - Eyebrow: `className="text-xs font-bold uppercase tracking-widest text-teal-400 mb-4"` — "What you walk away from the call with:"
    - Three items, each: `<div className="flex items-center gap-3 py-2 px-4 rounded-xl bg-white/5 border border-white/10 mb-2"><CheckCircle className="text-teal-400 w-4 h-4 flex-shrink-0" aria-hidden="true" /><span className="text-slate-300 text-sm">{item}</span></div>`
      - "A clear picture of what we'd build for your clinic"
      - "A rough timeline and what to expect"
      - "Honest advice — even if we're not the right fit"
    - Closing note: `className="text-slate-500 text-xs mt-4"` — "No pitch deck. No pressure. Just a real conversation."

**Critical rules:**
- No inline styles anywhere — all gradients via Tailwind `bg-gradient-*` or `bg-[radial-gradient(...)]` arbitrary syntax
- All internal navigation via `<Link to="...">` from react-router-dom — NEVER window.location.href
- `useLiteMotion()` called once at top of ForClinics(), never inside loops
- `variant="primary"` on ALL PremiumButton instances — never "orange"
- `aria-label` on every `<section>` element
- `aria-hidden="true"` on all decorative icons (X, CheckCircle in table rows, ArrowRight in inline links)
- Do NOT duplicate `/platform` content — keep all copy outcome-language only
  </action>

  <verify>
    <automated>cd "D:\VS Projects\Maple Tech Website 2.0" &amp;&amp; npm run build 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - ForClinics.jsx contains the string "Your software should fit"
    - ForClinics.jsx contains the string "useLiteMotion"
    - ForClinics.jsx contains the string 'to="/why-us#contact"' (at least 4 occurrences for the 4 full CTAs)
    - ForClinics.jsx does NOT contain "window.location.href"
    - ForClinics.jsx does NOT contain 'style={'
    - ForClinics.jsx contains "import ForClinicsFeatures"
    - ForClinics.jsx contains "Most clinics spend money to get new clients"
    - ForClinics.jsx contains "Off-the-shelf booking software"
    - ForClinics.jsx contains "What you walk away from the call with"
    - ForClinics.jsx contains 'variant="primary"'
    - ForClinics.jsx does NOT contain 'variant="orange"'
    - ForClinics.jsx contains "aria-label" (multiple occurrences — one per section)
    - src/components/sections/ForClinicsFeatures.jsx exists (stub — prevents import error)
    - npm run build exits 0 with no errors
  </acceptance_criteria>

  <done>ForClinics.jsx rebuilt from scratch with all 8 sections (Hero, Pain/Gaps, CRM grid, Comparison table, Social proof strip, Part 2 pivot, How it works, Final CTA), ForClinicsFeatures imported as stub, all CTAs to /why-us#contact, no inline styles, build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static page → CDN/browser | All content is static JSX — no user input, no server queries on this page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | Navigation CTAs | accept | All CTAs use React Router Link — no external redirect risk; destination is /why-us#contact on same domain |
| T-06-02 | Information Disclosure | Testimonial copy | accept | Representative quotes, no real PII; clearly representative clinic-owner voice, no client data exposed |
| T-06-03 | Denial of Service | Animation loops | mitigate | useLiteMotion() gate on all animated elements — disableInfiniteLoops prevents runaway CPU in reduced-motion environments |
</threat_model>

<verification>
1. `npm run build` exits 0 — no TypeScript/Vite errors
2. Navigate to /for-clinics in dev server — page renders without console errors
3. Hero headline visible: "Your software should fit your clinic. Not the other way around."
4. Five gap rows render with red X icons
5. Four CRM cards render with teal icon badges
6. Comparison table renders 5 rows with teal right column header
7. Three testimonial cards render with teal left border
8. Dark pivot section visible with "Most clinics spend money to get new clients." headline
9. Four timeline steps render on dark background with teal numbered circles
10. Final CTA section renders "Ready to build something your clients actually feel?"
</verification>

<success_criteria>
- /for-clinics loads and renders the full rebuilt page shell
- All sections Hero through Final CTA present with correct backgrounds per D-05
- ForClinicsFeatures stub imported (renders nothing yet — plan 06-02 will fill it)
- No console errors, no inline styles, no window.location.href
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-forclinics-wellness-aesthetics-redesign/06-01-SUMMARY.md`
</output>
