---
phase: 01-clean-and-fix
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/pages/Contact.jsx
  - src/App.jsx
  - src/pages/WhyUs.jsx
  - src/pages/Home.jsx
  - src/pages/Platform.jsx
  - src/pages/Automator.jsx
autonomous: true
requirements:
  - CLEAN-02
  - CLEAN-04
  - BUG-01
  - BUG-02

must_haves:
  truths:
    - "Contact.jsx does not exist in src/pages/"
    - "The /contact route is removed from App.jsx — no import, no <Route>"
    - "No file in src/ contains window.location.href as a navigation call"
    - "The WhyUs 'Local Advantage' section renders a Tailwind gradient — no prairie-hero.jpeg reference"
    - "WhyUs.jsx contains a useEffect that scrolls to #contact when the page loads with that hash"
    - "WhyUs.jsx same-page CTA buttons use scrollTo('contact') — not navigate() or window.location.href"
  artifacts:
    - path: "src/App.jsx"
      provides: "Navbar with navigate('/why-us#contact'), no Contact import, no /contact Route"
      contains: "useNavigate"
    - path: "src/pages/WhyUs.jsx"
      provides: "Hash scroll useEffect + gradient hero + scrollTo CTAs"
      contains: "window.location.hash"
    - path: "src/pages/Home.jsx"
      provides: "navigate('/why-us#contact') replacing window.location.href"
      contains: "navigate('/why-us#contact')"
    - path: "src/pages/Platform.jsx"
      provides: "navigate('/why-us#contact') replacing window.location.href"
      contains: "navigate('/why-us#contact')"
    - path: "src/pages/Automator.jsx"
      provides: "navigate('/why-us#contact') replacing window.location.href"
      contains: "navigate('/why-us#contact')"
  key_links:
    - from: "src/App.jsx Navbar scrollToContact()"
      to: "navigate('/why-us#contact')"
      via: "useNavigate hook"
      pattern: "navigate\\('/why-us#contact'\\)"
    - from: "src/pages/WhyUs.jsx"
      to: "document.getElementById('contact')"
      via: "useEffect on mount"
      pattern: "window\\.location\\.hash.*contact"
---

<objective>
Delete Contact.jsx, remove the /contact route, fix all remaining window.location.href navigations
across App.jsx / Home.jsx / Platform.jsx / Automator.jsx, fix the WhyUs same-page CTAs to use
scrollTo(), replace the broken prairie-hero.jpeg background with a Tailwind gradient, and add the
hash scroll useEffect to WhyUs.jsx.

Purpose: These six changes (D-01, D-02, D-03, D-04, D-08) collectively eliminate all broken
navigation, dead pages, missing images, and inline styles from Phase 1.
Output: Contact.jsx deleted; /contact route gone; zero window.location.href navigations in src/;
WhyUs renders gradient hero and scrolls to contact on hash navigation.
</objective>

<execution_context>
@D:/VS Projects/Maple Tech Website 2.0/.planning/phases/01-clean-and-fix/01-CONTEXT.md
@D:/VS Projects/Maple Tech Website 2.0/.planning/phases/01-clean-and-fix/01-02-SUMMARY.md
</execution_context>

<context>
@D:/VS Projects/Maple Tech Website 2.0/.planning/ROADMAP.md
@D:/VS Projects/Maple Tech Website 2.0/.planning/REQUIREMENTS.md

<interfaces>
<!-- App.jsx — Navbar scrollToContact() current shape (lines ~44–54): -->
<!--   function scrollToContact() { -->
<!--     if (location.pathname === '/why-us') { -->
<!--       document.getElementById('contact')?.scrollIntoView({ behavior: 'smooth' }); -->
<!--     } else { -->
<!--       window.location.href = '/why-us#contact';   <-- REPLACE THIS LINE -->
<!--     } -->
<!--   } -->
<!-- useLocation is already imported from react-router-dom. Add useNavigate to the same import. -->

<!-- App.jsx — Contact import (line ~10): -->
<!--   import Contact from './pages/Contact';    <-- DELETE this line -->
<!-- App.jsx — /contact Route (somewhere in route list): -->
<!--   <Route path="/contact" element={<Contact />} />   <-- DELETE this element -->

<!-- WhyUs.jsx — broken backgroundImage (lines ~86–93): -->
<!--   <motion.div -->
<!--     className="absolute inset-0 bg-cover bg-center" -->
<!--     style={{ backgroundImage: 'url("/prairie-hero.jpeg")' }} -->
<!--     initial={{ scale: 1.15 }} -->
<!--     whileInView={{ scale: 1 }} -->
<!--     viewport={{ once: true }} -->
<!--     transition={{ duration: 2, ease: [0.22, 1, 0.36, 1] }} -->
<!--   /> -->
<!-- REPLACE with (remove style prop, add Tailwind gradient class): -->
<!--   <motion.div -->
<!--     className="absolute inset-0 bg-gradient-to-br from-slate-800 via-slate-700 to-slate-900" -->
<!--     initial={{ scale: 1.15 }} -->
<!--     whileInView={{ scale: 1 }} -->
<!--     viewport={{ once: true }} -->
<!--     transition={{ duration: 2, ease: [0.22, 1, 0.36, 1] }} -->
<!--   /> -->

<!-- WhyUs.jsx — same-page CTAs (lines 62 and 201): -->
<!--   window.location.href = '/why-us#contact'   <-- REPLACE with: scrollTo('contact') -->
<!-- The scrollTo helper is already defined in WhyUs.jsx at lines ~20–25. -->

<!-- WhyUs.jsx — hash scroll useEffect to add: -->
<!--   useEffect(() => { -->
<!--     if (window.location.hash === '#contact') { -->
<!--       const el = document.getElementById('contact'); -->
<!--       if (el) el.scrollIntoView({ behavior: 'smooth' }); -->
<!--     } -->
<!--   }, []); -->
<!-- useEffect must be imported from React if not already present. -->

<!-- Home.jsx (line ~199): -->
<!--   window.location.href = '/why-us#contact'   <-- REPLACE with navigate('/why-us#contact') -->
<!-- useNavigate must be imported from react-router-dom and called at top of component. -->

<!-- Platform.jsx (line ~255): -->
<!--   window.location.href = '/why-us#contact'   <-- REPLACE with navigate('/why-us#contact') -->
<!-- useNavigate must be imported from react-router-dom and called at top of component. -->

<!-- Automator.jsx (line ~795): -->
<!--   window.location.href = '/why-us#contact'   <-- REPLACE with navigate('/why-us#contact') -->
<!-- RESEARCH confirms useNavigate is already imported at line 2 of Automator.jsx. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Contact.jsx and remove /contact route from App.jsx (per D-03, D-04)</name>
  <read_first>
    Read: src/App.jsx — find:
      (a) The import of Contact at line ~10: `import Contact from './pages/Contact'`
      (b) The Route element for /contact in the route list
      (c) The Navbar scrollToContact() function at lines ~44–54 (to prepare for Task 2)
      (d) The useLocation import from react-router-dom (line ~2) — Task 2 adds useNavigate here
    Confirm: src/pages/WhyUs.jsx line 17 — should already show ContactFormSection import (from Plan 02).
      If it still shows `from './Contact'`, STOP and report. Plan 02 must complete first.
  </read_first>
  <files>src/pages/Contact.jsx, src/App.jsx</files>
  <action>
    Step 1 — Verify Plan 02 is complete:
      Run `grep "ContactFormSection" src/pages/WhyUs.jsx`
      If this returns zero matches, STOP. Plan 02 has not run. Do not proceed.

    Step 2 — Delete Contact.jsx:
      `rm "src/pages/Contact.jsx"`

    Step 3 — Remove Contact import from App.jsx:
      Find the line: `import Contact from './pages/Contact'` (line ~10)
      Delete that entire line.

    Step 4 — Remove the /contact Route from App.jsx:
      Find the JSX element: `<Route path="/contact" element={<Contact />} />`
      Delete that entire element (it may span 1–3 lines depending on formatting).

    Step 5 — Run build to confirm no dangling references:
      `npm run build`
      If the build fails with "cannot find module Contact" in any file OTHER than App.jsx,
      investigate and fix that file before proceeding.
  </action>
  <verify>
    <automated>ls src/pages/Contact.jsx 2>&1 | grep -q "No such file" && echo "DELETED OK" || echo "STILL EXISTS"</automated>
    <automated>grep -rn "from.*pages/Contact\|from.*./Contact" src/ 2>/dev/null</automated>
    <automated>grep -n "path=\"/contact\"" src/App.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/pages/Contact.jsx` returns "No such file or directory"
    - `grep -rn "from.*pages/Contact\|from.*./Contact" src/` returns ZERO matches (WhyUs already points to ContactFormSection)
    - `grep "path=\"/contact\"" src/App.jsx` returns ZERO matches
    - `grep "import Contact from" src/App.jsx` returns ZERO matches
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Contact.jsx is deleted. The /contact route and its import are removed from App.jsx. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all window.location.href navigations — App.jsx, Home.jsx, Platform.jsx, Automator.jsx (per D-02)</name>
  <read_first>
    Read: src/App.jsx lines 1–60 — find the useLocation import at line ~2, and the scrollToContact() function at lines ~44–54.
    Read: src/pages/Home.jsx lines 190–210 — find window.location.href at line ~199 and confirm useNavigate import status.
    Read: src/pages/Platform.jsx lines 248–265 — find window.location.href at line ~255 and confirm useNavigate import status.
    Read: src/pages/Automator.jsx lines 1–10 and 788–800 — confirm useNavigate import at line 2; find window.location.href at line ~795.
  </read_first>
  <files>src/App.jsx, src/pages/Home.jsx, src/pages/Platform.jsx, src/pages/Automator.jsx</files>
  <action>
    === src/App.jsx — Navbar scrollToContact() ===

    1. Find the import at line ~2:
         `import { useLocation } from 'react-router-dom';`
       Add `useNavigate` to the import:
         `import { useLocation, useNavigate } from 'react-router-dom';`
       NOTE: This import is inside (or just before) the Navbar function — confirm it is in a position
       where hooks can be called (inside a component function body or at module scope). The Navbar
       renders inside AppContent which is inside Router, so useNavigate is safe here.

    2. After the `useLocation` call (or wherever other hooks are called in Navbar), add:
         `const navigate = useNavigate();`

    3. Inside scrollToContact(), find:
         `window.location.href = '/why-us#contact';`
       Replace with:
         `navigate('/why-us#contact');`

    === src/pages/Home.jsx ===

    4. Find the existing imports from react-router-dom. If `useNavigate` is not imported, add it.
       At the top of the relevant component function, add: `const navigate = useNavigate();`
    5. Find line ~199: `window.location.href = '/why-us#contact'`
       Replace with: `navigate('/why-us#contact')`

    === src/pages/Platform.jsx ===

    6. Same pattern as Home.jsx — add useNavigate import if missing, add `const navigate = useNavigate();`
       inside the component, then replace line ~255: `window.location.href = '/why-us#contact'`
       with: `navigate('/why-us#contact')`

    === src/pages/Automator.jsx ===

    7. useNavigate is already imported at line 2 (RESEARCH verified). Find the scrollToContact()
       function at line ~795 and replace: `window.location.href = '/why-us#contact'`
       with: `navigate('/why-us#contact')`
       Confirm `const navigate = useNavigate();` already exists in Automator() — if not, add it
       at the top of the Automator() function body (before any JSX).

    After all four files are updated, run: `grep -rn "window.location.href" src/`
    Expected: ZERO matches.
  </action>
  <verify>
    <automated>grep -rn "window.location.href" src/</automated>
    <automated>grep -n "useNavigate" src/App.jsx</automated>
    <automated>grep -n "useNavigate" src/pages/Home.jsx</automated>
    <automated>grep -n "useNavigate" src/pages/Platform.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "window.location.href" src/` returns ZERO matches
    - `grep "useNavigate" src/App.jsx` returns at least two matches (import + hook call)
    - `grep "navigate('/why-us#contact')" src/App.jsx` returns one match (in scrollToContact)
    - `grep "navigate('/why-us#contact')" src/pages/Home.jsx` returns one match
    - `grep "navigate('/why-us#contact')" src/pages/Platform.jsx` returns one match
    - `grep "navigate('/why-us#contact')" src/pages/Automator.jsx` returns one match
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>All window.location.href navigation calls are replaced with React Router navigate(). The back button works correctly throughout the site.</done>
</task>

<task type="auto">
  <name>Task 3: Fix WhyUs.jsx — gradient hero, same-page scrollTo CTAs, hash scroll useEffect (per D-01, D-02, BUG-01, BUG-02)</name>
  <read_first>
    Read: src/pages/WhyUs.jsx lines 1–30 — check existing imports (React, useEffect, motion, scrollTo helper definition).
    Read: src/pages/WhyUs.jsx lines 55–70 — find the "Get Started" button at line ~62 with window.location.href.
    Read: src/pages/WhyUs.jsx lines 84–100 — find the broken backgroundImage motion.div at lines ~86–93.
    Read: src/pages/WhyUs.jsx lines 195–210 — find the "Book Your Free Consultation" button at line ~201 with window.location.href.
  </read_first>
  <files>src/pages/WhyUs.jsx</files>
  <action>
    === Fix 1: Remove broken prairie-hero.jpeg backgroundImage (BUG-01, D-01) ===

    Find the motion.div at lines ~86–93 that has:
      `className="absolute inset-0 bg-cover bg-center"`
      `style={{ backgroundImage: 'url("/prairie-hero.jpeg")' }}`

    Replace the ENTIRE motion.div opening tag + style prop with:
      `<motion.div`
      `  className="absolute inset-0 bg-gradient-to-br from-slate-800 via-slate-700 to-slate-900"`

    Keep the animation props unchanged:
      `initial={{ scale: 1.15 }}`
      `whileInView={{ scale: 1 }}`
      `viewport={{ once: true }}`
      `transition={{ duration: 2, ease: [0.22, 1, 0.36, 1] }}`

    The existing dark overlay div immediately after (the `bg-gradient-to-t from-slate-900...` div)
    remains completely unchanged.

    === Fix 2: Replace same-page window.location.href with scrollTo() (D-02) ===

    Find line ~62 (inside "Get Started" PremiumButton onClick):
      `window.location.href = '/why-us#contact'`
    Replace with: `scrollTo('contact')`

    Find line ~201 (inside "Book Your Free Consultation" PremiumButton onClick):
      `window.location.href = '/why-us#contact'`
    Replace with: `scrollTo('contact')`

    The `scrollTo` helper is already defined in WhyUs.jsx (lines ~20–25). Do NOT import navigate()
    for these two buttons — they are same-page scrolls, not cross-route navigations.

    === Fix 3: Add hash scroll useEffect (D-02 complement) ===

    Ensure `useEffect` is imported from React. Find the existing import at line ~1:
      `import React from 'react';`
    If useEffect is not already in the import, update to:
      `import React, { useEffect } from 'react';`
    (Or add to an existing destructured import if present.)

    Inside the WhyUs() component function, after all existing hook calls and before the return statement,
    add this useEffect:
      ```
      useEffect(() => {
        if (window.location.hash === '#contact') {
          const el = document.getElementById('contact');
          if (el) el.scrollIntoView({ behavior: 'smooth' });
        }
      }, []);
      ```

    This runs once on mount. When a user clicks "Get Started" from Home/Platform/Automator and arrives
    at WhyUs via navigate('/why-us#contact'), this useEffect fires and scrolls to the contact section.

    === Fix 4: Confirm hooks ordering (BUG-02 scan) ===

    Scan WhyUs.jsx for any useState or useEffect calls that appear AFTER conditional logic or early returns.
    With this plan's changes, the new useEffect is added at the top of the function body — it must be
    placed before any conditional rendering, not inside an if block.
  </action>
  <verify>
    <automated>grep -n "prairie-hero" src/pages/WhyUs.jsx</automated>
    <automated>grep -n "bg-gradient-to-br from-slate-800" src/pages/WhyUs.jsx</automated>
    <automated>grep -n "window.location.href" src/pages/WhyUs.jsx</automated>
    <automated>grep -n "window.location.hash" src/pages/WhyUs.jsx</automated>
    <automated>grep -n "scrollTo('contact')" src/pages/WhyUs.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "prairie-hero" src/pages/WhyUs.jsx` returns ZERO matches
    - `grep "bg-gradient-to-br from-slate-800" src/pages/WhyUs.jsx` returns one match (on the motion.div)
    - `grep "window.location.href" src/pages/WhyUs.jsx` returns ZERO matches
    - `grep "window.location.hash" src/pages/WhyUs.jsx` returns one match (in the useEffect)
    - `grep "scrollTo('contact')" src/pages/WhyUs.jsx` returns two matches (the two same-page CTAs)
    - `grep "useEffect" src/pages/WhyUs.jsx` returns at least one match (the hash scroll effect)
    - The new useEffect is placed inside the WhyUs() function body before any return statement
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>
    WhyUs.jsx: gradient replaces broken image; same-page CTAs use scrollTo; hash scroll useEffect fires
    on mount so cross-route navigation to /why-us#contact scrolls to the contact section. No window.location.href remains.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Netlify Forms | ContactForm (now in ContactFormSection.jsx) submits via fetch. No change to this flow. |
| Internal SPA routing | All navigate() calls are internal — no external URLs, no open redirects possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | navigate() calls | accept | All navigate() targets are hardcoded internal paths ('/why-us#contact'). No user-controlled input enters the navigate() call. |
| T-03-02 | Elevation of Privilege | /contact route removal | accept | Removing a public route reduces attack surface. No auth logic involved. |
| T-03-03 | Denial of Service | useEffect hash scroll | accept | useEffect runs once on mount with getElementById — safe DOM query, no user input in the selector |
</threat_model>

<verification>
Full post-task verification:

```bash
# No window.location.href anywhere in src/
grep -rn "window.location.href" src/

# Contact.jsx gone
ls src/pages/Contact.jsx 2>&1

# No /contact route or import in App.jsx
grep "contact" src/App.jsx | grep -i "route\|import Contact"

# WhyUs gradient present
grep "bg-gradient-to-br from-slate-800" src/pages/WhyUs.jsx

# Hash scroll useEffect present
grep "window.location.hash" src/pages/WhyUs.jsx

# Same-page CTAs use scrollTo
grep "scrollTo('contact')" src/pages/WhyUs.jsx

# Build passes
npm run build
```
</verification>

<success_criteria>
- `grep -rn "window.location.href" src/` returns ZERO matches
- `ls src/pages/Contact.jsx` returns "No such file or directory"
- `grep "path=\"/contact\"" src/App.jsx` returns ZERO matches
- `grep "prairie-hero" src/pages/WhyUs.jsx` returns ZERO matches
- `grep "bg-gradient-to-br from-slate-800" src/pages/WhyUs.jsx` returns one match
- `grep "window.location.hash" src/pages/WhyUs.jsx` returns one match
- `grep "scrollTo('contact')" src/pages/WhyUs.jsx` returns two matches
- `npm run build` exits with code 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-clean-and-fix/01-03-SUMMARY.md` with:
- Confirmation of Contact.jsx deletion and /contact route removal
- List of all files where window.location.href was replaced (and with what)
- Confirmation of gradient class added to WhyUs.jsx
- Confirmation of hash scroll useEffect added
- Build result
</output>
