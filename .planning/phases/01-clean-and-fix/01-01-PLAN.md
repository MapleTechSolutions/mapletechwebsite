---
phase: 01-clean-and-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MapleTechPremium.jsx
  - src/App.jsx
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-05

must_haves:
  truths:
    - "MapleTechPremium.jsx does not exist anywhere in src/"
    - "The footer displays the current year dynamically — not the hardcoded string 2025"
  artifacts:
    - path: "src/App.jsx"
      provides: "Footer component with dynamic copyright year"
      contains: "new Date().getFullYear()"
  key_links:
    - from: "src/App.jsx Footer()"
      to: "runtime JS expression"
      via: "{new Date().getFullYear()}"
      pattern: "new Date\\(\\)\\.getFullYear\\(\\)"
---

<objective>
Delete the dead MapleTechPremium.jsx file (1,617 lines with zero live imports) and replace
the hardcoded `© 2025` in the App.jsx Footer with a dynamic `{new Date().getFullYear()}` expression.

Purpose: Eliminate dead bundle weight and prevent the copyright year from going stale.
Output: MapleTechPremium.jsx removed from repo; App.jsx Footer updated.
</objective>

<execution_context>
@D:/VS Projects/Maple Tech Website 2.0/.planning/phases/01-clean-and-fix/01-CONTEXT.md
</execution_context>

<context>
@D:/VS Projects/Maple Tech Website 2.0/.planning/ROADMAP.md
@D:/VS Projects/Maple Tech Website 2.0/.planning/REQUIREMENTS.md

<interfaces>
<!-- From src/App.jsx Footer (line ~199): -->
<!-- Current: <p>© 2025 Maple Tech Solutions. All rights reserved.</p> -->
<!-- Target:  <p>© {new Date().getFullYear()} Maple Tech Solutions. All rights reserved.</p> -->
<!-- Footer() is defined at lines ~180–208 in App.jsx -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete MapleTechPremium.jsx (per D-05)</name>
  <read_first>
    Run: grep -rn "MapleTechPremium" "src/" to confirm zero imports before deleting.
    Read: src/components/MapleTechPremium.jsx — confirm it exists and is the right file.
  </read_first>
  <files>src/components/MapleTechPremium.jsx</files>
  <action>
    1. Run `grep -rn "MapleTechPremium" src/` and confirm the ONLY match is inside MapleTechPremium.jsx itself (its own function/class name). If any OTHER file imports it, STOP and report — do not delete.
    2. Delete the file: `rm "src/components/MapleTechPremium.jsx"`
    3. Run `npm run build` to confirm the build still passes with the file removed.
  </action>
  <verify>
    <automated>ls src/components/MapleTechPremium.jsx 2>&1 | grep -q "No such file" && echo "DELETED OK" || echo "FILE STILL EXISTS"</automated>
    <automated>grep -r "MapleTechPremium" src/ && echo "STILL REFERENCED" || echo "NO REFERENCES"</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/components/MapleTechPremium.jsx` returns "No such file or directory"
    - `grep -r "MapleTechPremium" src/` returns zero matches
    - `npm run build` exits with code 0 (no build errors)
  </acceptance_criteria>
  <done>MapleTechPremium.jsx is absent from the repo and the build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Fix footer copyright year (per D-06)</name>
  <read_first>
    Read: src/App.jsx lines 180–210 — see the Footer() component and the exact copyright string at line ~199.
  </read_first>
  <files>src/App.jsx</files>
  <action>
    In src/App.jsx, locate the Footer() component (around lines 180–208).
    Find the exact string: `© 2025 Maple Tech Solutions. All rights reserved.`
    Replace the paragraph element with:
    `&lt;p&gt;© {new Date().getFullYear()} Maple Tech Solutions. All rights reserved.&lt;/p&gt;`

    The JSX expression `{new Date().getFullYear()}` evaluates at render time and always returns the current year.
    Do NOT use a hardcoded number. Do NOT introduce a state variable or useEffect — a plain inline expression is sufficient.
  </action>
  <verify>
    <automated>grep -n "new Date().getFullYear()" src/App.jsx</automated>
    <automated>grep -n "2025" src/App.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "new Date().getFullYear()" src/App.jsx` returns at least one match inside the Footer component
    - `grep "© 2025" src/App.jsx` returns zero matches (the hardcoded year is gone)
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Footer renders © {current year} dynamically. The string "© 2025" no longer appears anywhere in App.jsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None introduced | This plan performs file deletion and a one-line string replacement — no new data flows or trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | File deletion step | accept | Verified by grep before delete; build confirms no breakage |
| T-01-02 | Information Disclosure | Dead code in bundle | mitigate | MapleTechPremium.jsx removal shrinks attack surface; no sensitive data was in the file |
</threat_model>

<verification>
After both tasks complete:

```bash
# Confirm dead file is gone
ls src/components/MapleTechPremium.jsx 2>&1

# Confirm no lingering references
grep -r "MapleTechPremium" src/

# Confirm dynamic year
grep "getFullYear" src/App.jsx

# Confirm no hardcoded 2025 in footer area
grep "2025" src/App.jsx

# Confirm build passes
npm run build
```
</verification>

<success_criteria>
- MapleTechPremium.jsx does not exist in the repository
- `grep -r "MapleTechPremium" src/` returns zero matches
- `grep "© 2025" src/App.jsx` returns zero matches
- `grep "new Date().getFullYear()" src/App.jsx` returns one match inside Footer()
- `npm run build` exits with code 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-clean-and-fix/01-01-SUMMARY.md` with:
- What was deleted and confirmed zero imports
- Exact line changed in App.jsx for copyright
- Build result confirmation
</output>
